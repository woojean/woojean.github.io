# 类迭代器

可以通过foreach来循环得到类的所有属性：

```php
class myClass{
public $a = ‘1’;
public $b = ‘2’;
public $c = ‘3’;
}
$x = new myClass;
foreach( $x as $attribute){
echo $attribute;
}
```
输出：123

可以结合使用Iterator和IteratorAggregate接口自定义更加复杂的类属性遍历行为。